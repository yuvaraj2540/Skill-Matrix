setTimeout(function(){
	QUnit.start();
}, 1000)


QUnit.testStart(function( test ) {
    //Perform any screen automation tasks here
    if(test.name == "test name"){
       
    }
});

QUnit.testDone(function( test ) {
    //Perform any screen automation tasks here
   
    //Uncomment below code and replace the test name in the below condition with the test name after which the flow automation should happen.
    /*
    if(test.name == "test name"){
         console.log("First test complete. Starting sub product test.")
   
    }
    */
   
    // Uncomment the below code and replace the test name in the below condition with the last test name in the flow. This is to generate the Qunit test report.
    /*
    else if(test.name == "test name"){
        console.log("Generating report")
        downloadQunitReport();
    }
    */
});
